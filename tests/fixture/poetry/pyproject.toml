[project]
name = "update-poetry-dependencies-fixture"
version = "0.1.0"
description = "Fixture project for the update-poetry-dependencies action e2e test."
requires-python = ">=3.10,<4.0"
dependencies = [
    "idna (>=3.4,<4.0)",
    "six (>=1.15.0,<2.0)",
]

[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
